<%- include("header") -%>
    <div id="compare-teams" class="before-information">
        <table>
            <tr>
                <th>Team 1 Name</th>
                <th>Team 2 Name</th>
            </tr>
            <% for (var i = 0; i < statEndings.length; i ++) { %>
                <tr>
                    <td> Team 1 <%= statEndings[i] %></td>
                    <td>Team 2 <%= statEndings[i] %></td>
                </tr>
            <% } %>
            
        </table>
    </div>
<%- include("footer") -%>